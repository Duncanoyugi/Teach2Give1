// app.ts
// A simple demonstration of a TypeScript application

// Function to greet a user
function greetUser(name: string): string {
    return `Hello, ${name}! Welcome to the TypeScript app.`;
}

// Example usage
const userName = "John";
console.log(greetUser(userName));